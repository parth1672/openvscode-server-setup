# Setup is for multiple openvscode-server in same server with different working directory on different port
- Set environment variable in .env file
- Run using : docker compose up -d
- Open code : ip:CODE_PORT